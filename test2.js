//socket.on('sendMessage',function (data) {
 //   console.log(data.to);
 //	 console.log(data.message);
   // console.log(data.from);
 //	 $name="testme";
   //  for (var i in users) {
   //          //var s = io.sockets.connected[i];
   //          if (users[i].name === data.to) {
   // 					 console.log(users[i].id);
   //             io.sockets.connected[users[i].id].emit('newMessage', 'for your eyes only');
   //          }
   //          console.log("User not available");
   //      }

  //var id= data.to;
 // users[id] = socket;
 // users[id].emit('newMessage',$name);
 // io.to(id).emit('newMessage',$name);  //no effect
 //socket.to(id).emit('newMessage',$name); //no effect
 //  io.sockets.socket[id].emit('newMessage',$name); error
  //io.sockets.(id).emit('newMessage',$name); error



  // if(io.nsps['/'].adapter.rooms["room-"+roomno] && io.nsps['/'].adapter.rooms["room-"+roomno].length > 1)
  // 	 roomno++;
  //  socket.join("room-"+roomno);
  // var $name="check";
  //  //Send this event to everyone in the room.
  //  io.sockets.in("room-"+roomno).emit('newMessage', $name);
  	//	io.sockets.users(t).emit('newMessage',data.message);
  	//	io.sockets.t.emit('newMessage',data.message);
      //users[data.to].emit('newMessage',data.message);
  //io.sockets.connected(data.to).emit('newMessage',data.message);
  	 //io.sockets.socket(data.to).emit('newMessage',data.message);
                //to everyone online
     //socket.broadcast.emit('newMessage',$name ); //works fine
  	  //socket.emit('newMessage',$name); //works fine
